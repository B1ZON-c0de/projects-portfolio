# 🚀 Blog Platform — быстро, удобно и современно

Минималистичная платформа блогов с аутентификацией, ролями и управлением контентом. Запускается за минуты и радует отличным DX.

## 🔑 Демо-учётная запись администратора

- **Логин**: `admin`
- **Пароль**: `123456`

Используйте для входа и доступа к административным функциям.

## ✨ Что умеет

- **Посты**: создание, редактирование, удаление, список и детализация
- **Комментарии**: добавление и модерация
- **Аутентификация**: регистрация, вход, роли (пользователь/админ)
- **Панель администратора**: управление пользователями и контентом
- **Валидация форм** и удобный UX

## 🧩 Архитектура

- **Frontend**: Vite + Vue 3 + TypeScript + Pinia + Vue Router + Tailwind CSS + Vee-Validate
- **Backend**: Node.js + Express + MongoDB (Mongoose), JWT, Cookies
- **Тесты**: Vitest, Cypress (структура подготовлена)

## 🧰 Стек технологий

- Vue 3, TypeScript, Vite, Pinia, Vue Router
- Tailwind CSS
- Vee-Validate, Yup
- Node.js, Express, Mongoose, JWT, cookie-parser, bcrypt, dotenv

## ⚙️ Требования

- Node.js 20+ (см. `engines` в `frontend/package.json`)
- MongoDB Community Server

## 📦 Установка

1. Установите зависимости фронтенда:

   - Перейдите в папку `frontend` и выполните:

     ```bash
     npm install
     ```

2. Установите зависимости бэкенда:

   - Перейдите в папку `backend` и выполните:

     ```bash
     npm install
     ```

## 🔐 Переменные окружения

- Backend (`backend/.env`):
  - `DB_CONNECTION_STRING=mongodb://localhost:27017/blog`
- Frontend (`frontend/.env`):
  - `VITE_YANDEX_WEATHER_API_KEY=...` (пример уже присутствует)

## ▶️ Запуск (режим разработки)

Откройте два терминала.

- **Frontend (Vite)**

  - Папка: `frontend`
  - Команда:

    ```bash
    npm run dev
    ```

  - По умолчанию: <http://localhost:5173>

- **Backend (Express)**

  - Папка: `backend`
  - Команда:

    ```bash
    npm run dev
    ```

  - Сервер поднимется на порту `3002` (см. `backend/app.js`)

Убедитесь, что MongoDB запущен и строка подключения в `.env` корректна.

## 🧪 Тестирование

- Юнит-тесты (Vitest): структура подготовлена. Запуск при необходимости через `vitest`.
- E2E (Cypress): структура подготовлена.

## 🏗️ Сборка фронтенда

- Команда (в папке `frontend`):

  ```bash
  npm run build
  ```

- Предпросмотр:

  ```bash
  npm run preview
  ```

## 🗺️ Полезные порты и пути

- Frontend (Vite): <http://localhost:5173>
- Backend (API): <http://localhost:3002>
- Точка входа backend: `backend/app.js`

## 🙌 Подсказки

- Если фронтенд не видит бэкенд, проверьте CORS/прокси и используемые API URL.
- Проверьте, чтобы MongoDB был запущен перед стартом бэкенда.

## 📄 Лицензия

[MIT](https://choosealicense.com/licenses/mit/)
